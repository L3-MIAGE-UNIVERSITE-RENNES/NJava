package test;
public class Circle {
    static public double perimetre(double r){
        return 2*Math.PI*r;
    }

    static public double surface(double r){
        return Math.PI*Math.pow(r, 2);
    }
}
